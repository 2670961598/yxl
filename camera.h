#ifndef CAMERA_H
#define CAMERA_H


#include <QMainWindow>
#include <QCameraInfo>

class camera : public QWidget
{
    Q_OBJECT
public:
    explicit camera(QWidget *parent = nullptr);


signals:

private slots:

};

#endif // CAMERA_H
